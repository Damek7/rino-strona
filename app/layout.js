export const metadata = {
  title: 'RinoMove',
  description: 'Trenerzy, rezerwacje i płatności w jednym miejscu.',
}

export default function RootLayout({ children }) {
  return <html lang="pl"><body>{children}</body></html>
}
